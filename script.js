
testing function data(e){
    let x = await fetch("DYNAMICVALUE");
   DYNAMICVALUE;
    let user=x[e.target.name];
   DYNAMICVALUE("main").style.DYNAMICVALUE="turquoise";
   DYNAMICVALUE("name").innerHTML="Name: "+user.name;
   DYNAMICVALUE("email").innerHTML="Email: "+user.email;
   DYNAMICVALUE("skill").innerHTML="Skill: "+user.skill;
   DYNAMICVALUE("state").innerHTML="State: "+user.state;
   DYNAMICVALUE("company").innerHTML="Company: "+ user.company;

}

